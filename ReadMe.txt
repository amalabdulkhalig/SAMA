# Image detection Dash app 

This web app is built to detect objects in images using deep learning model Convolutional Neural Network (CNN)
tensorflow.keras was utilized to build the model
the model was then saved into (Model) folder and clled from the web application  
the web application was built using Dash framework 
